import spinner from "./spinner.png";
const Spinner = () => {
  return (
    <img
      style={{
        margin: "60px auto",
        background: "none",
        display: "block",
      }}
      src={spinner}
      alt="spinner"
    />
  );
};

export default Spinner;
